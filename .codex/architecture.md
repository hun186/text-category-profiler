# Architecture

> 類型：Current state。描述目前有效的系統模型；不要追加按日期排列的改動紀錄。

## 系統邊界

- 系統負責：`待初始化`
- 系統不負責：`待初始化`
- 上游：`待初始化`
- 下游：`待初始化`

## 高階元件

| 元件／模組 | 責任 | 輸入 | 輸出 | 主要位置 |
| --- | --- | --- | --- | --- |
| 待初始化 | 待初始化 | 待初始化 | 待初始化 | `待初始化` |

## 主要資料流

以 3～8 個步驟描述正常路徑；需要表達分支、回復或多 consumer 時才加入精簡 Mermaid 圖。

1. `待初始化`

## 依賴方向與模組邊界

- `待初始化：例如 domain 不依賴 web framework，或 pipeline stage 只能透過 contract 交接。`
- 跨模組共用邏輯的位置：`待初始化`
- 禁止的依賴或直接存取：`待初始化`

## 核心資料模型與狀態

| 名稱 | 意義 | 擁有者 | 儲存／生命週期 | 權威定義 |
| --- | --- | --- | --- | --- |
| 待初始化 | 待初始化 | 待初始化 | 待初始化 | `待初始化` |

## 不變條件

只有必須始終成立、且違反會造成錯誤或相容性問題的條件才放這裡。

- `待初始化`

## 錯誤、重試與回復邊界

- 失敗如何傳遞：`待初始化`
- 重試責任：`待初始化`
- idempotency／去重：`待初始化`
- rollback／補償：`待初始化`

## 安全與信任邊界

- 身分與權限：`待初始化`
- 敏感資料：`待初始化；只描述分類與處理規則，不寫實際值`
- 外部輸入驗證：`待初始化`
- 高風險操作：`待初始化`

## 效能與營運邊界

- 主要規模假設：`待確認`
- 可能的瓶頸：`待確認`
- 可觀測性：`待初始化`
- 已知部署限制：`待初始化`

## 架構導覽

| 想修改的能力 | 優先查看 | 相關測試／契約 |
| --- | --- | --- |
| 待初始化 | `待初始化` | `待初始化` |

## 維護規則

- 模組責任、資料流、依賴方向、信任邊界或不變條件改變時原地更新。
- 不在本檔保存「某日新增某檔」的工作紀錄；結果放 `memory.md`，原因與取捨放 `decisions.md`。
- 若現況與舊決策不同，先確認是實作偏離還是決策被取代，再同步兩者。
